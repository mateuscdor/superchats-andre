import { Boom } from '@hapi/boom';
import { AuthenticationState } from "../model";
import { BinaryNode as BinaryNodeM } from '../bin';
import { proto } from '../binary';
export declare const decodeMessageStanza: (stanza: BinaryNodeM, auth: AuthenticationState) => Promise<{
    msgId: string;
    chatId: string;
    author: string;
    from: string;
    timestamp: number;
    participant: string;
    recipient: string;
    pushname: string;
    successes: proto.Message[];
    failures: {
        error: Boom;
    }[];
}>;
